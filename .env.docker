# Environment variables for Docker Compose

# Node Environment
NODE_ENV=production

# Backend Port
PORT=5000

# Database Configuration
DB_HOST=db
DB_PORT=3306
DB_USER=asset_user
DB_PASSWORD=secure_password_change_me
DB_NAME=asset_lifecycle_db

# JWT Secret - CHANGE THIS IN PRODUCTION!
JWT_SECRET=your_super_secret_jwt_key_change_in_production_min_32_chars

# Database URL (Prisma)
DATABASE_URL=mysql://asset_user:secure_password_change_me@db:3306/asset_lifecycle_db

# Frontend API URL (OPTIONAL - auto-detected by default)
# Frontend automatically uses browser's host:5000 for API calls
# Leave commented for automatic detection
# Only uncomment if you need a specific custom API URL
# VITE_API_URL=http://custom-domain.com:5000

# Frontend Configuration
VITE_API_URL=http://localhost:5000
